package com.company;

public class Controller {
    static int summ(){ return Model.x+Model.y;}
    static int difference(){ return Model.x-Model.y; }
    static int product(){ return Model.x*Model.y; }
    static double division(){ return (double)Model.x/(double)Model.y; }
}
